[project]
name = "ai-system-design-consultent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "langchain>=1.3.14",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.2.10",
    "langsmith>=0.10.15",
    "python-dotenv>=1.0.1",
    "uvicorn>=0.52.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

